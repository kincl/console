name: Docker Image CI

on:
  push:
    branches: [ "release-4.10" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile.plugins.demo --tag quay.io/jkincl/console-demo-plugin:latest
